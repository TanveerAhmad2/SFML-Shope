#include <SFML/Graphics.hpp>

using namespace sf;
int main(){
    RenderWindow tk(VideoMode(300, 200),"Title Of Window");
    tk.setFramerateLimit(50);
    while(tk.isOpen())
    {
        ///******************************************
    /// Just Click On Console Window Close Programm *
       ///*******************************************
    }
    return EXIT_SUCCESS;
}
